name: Live extractor check

on:
  schedule:
    # 06:00 UTC = 07:00 CET / 08:00 CEST. Water tariffs are annual so the
    # daily run is mostly a "did the page change shape" canary.
    - cron: "0 6 * * *"
  workflow_dispatch:

permissions:
  contents: read
  issues: write

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: "3.13"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pypdf pdfplumber aiohttp beautifulsoup4

      - name: Run live check (up to 5 attempts on extractor failure)
        id: check
        run: |
          set +e
          attempts=5
          delays="10 30 60 120"
          set -- $delays
          for i in $(seq 1 "$attempts"); do
            echo "::group::attempt $i"
            python scripts/live_check.py > report.md
            rc=$?
            cat report.md
            echo "::endgroup::"
            # Stop once fully green. Retry on any failure, bit 1 (real)
            # or bit 2 (transient), so an upstream hiccup gets its
            # retries to recover before we judge the final state.
            if [ $((rc & 3)) -eq 0 ]; then
              break
            fi
            if [ "$i" -lt "$attempts" ]; then
              sleep_for=$1
              shift
              echo "attempt $i failed (exit $rc), sleeping ${sleep_for}s..." >&2
              sleep "$sleep_for"
            fi
          done
          echo "rc=$rc" >> "$GITHUB_OUTPUT"
          # Only a real failure (bit 1) opens an issue; a transient-only
          # result (bit 2) is an upstream hiccup, not a regression.
          if [ $((rc & 1)) -ne 0 ]; then
            echo "real_fail=1" >> "$GITHUB_OUTPUT"
          else
            echo "real_fail=0" >> "$GITHUB_OUTPUT"
          fi
          # Exit 0 here so subsequent issue-creation steps run regardless.
          exit 0

      - name: Open or update extractor-broken issue
        if: steps.check.outputs.real_fail == '1'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          title_prefix='[live-check] water extractor broken'
          run_url="${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          {
            echo "## Run"
            echo
            echo "$run_url"
            echo
            echo "## Last report"
            echo
            cat report.md
          } > issue_body.md

          existing=$(gh issue list \
            --state open \
            --search "\"$title_prefix\" in:title" \
            --json number,title \
            --jq '.[0].number // empty')

          if [ -n "$existing" ]; then
            echo "appending comment to issue #$existing"
            gh issue comment "$existing" --body-file issue_body.md
          else
            gh issue create \
              --title "$title_prefix ($(date -u +%Y-%m-%d))" \
              --body-file issue_body.md
          fi
